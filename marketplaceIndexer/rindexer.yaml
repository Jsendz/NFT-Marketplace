name: marketplaceIndexer
project_type: no-code

networks:
  - name: sepolia
    chain_id: 11155111
    rpc: ${RPC_URL}

storage:
  postgres:
    enabled: true

contracts:
  - name: NftMarketplace
    details:
      - network: sepolia
        address: "0x91c7ed5261a7e578ebd458efb7f17d04108ecffa"  # QUOTED
        start_block: 9039207                                    # DECIMAL (no 0x)
    abi: ./abis/nftMarketplace.abi.json
    include_events:
      - ItemListed
      - ItemCanceled
      - ItemBought

graphql:
  host: 0.0.0.0
  port: 3001
